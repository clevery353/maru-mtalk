<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="chatattach">
	<insert id="attachInsert" parameterType="chatAttachVO" useGeneratedKeys="true">
		INSERT INTO CHATATTACH 
		(
			CHATBUBBLE_ID,
			CHATATTACH_ORIGINAL,
			CHATATTACH_PATH,
			CHATATTACH_URL,
			CHATATTACH_MIME,
			CHATATTACH_SIZE,
			CHATATTACH_FANCY
		) VALUES (
			#{chatbubble_id},
			#{chatattach_original},
			#{chatattach_path},
			#{chatattach_url},
			#{chatattach_mime},
			#{chatattach_size},
			#{chatattach_fancy}
		)
	</insert>
	<select id="attachSelect" parameterType="String" resultType="chatAttachVO">
		SELECT 
			CHATATTACH_ID,
			CHATBUBBLE_ID,
			CHATATTACH_ORIGINAL,
			CHATATTACH_PATH,
			CHATATTACH_URL,
			CHATATTACH_MIME,
			CHATATTACH_SIZE,
			CHATATTACH_FANCY
		FROM CHATATTACH
		WHERE CHATBUBBLE_ID = #{chatbubble_id}
	</select>
</mapper>